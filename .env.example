# Puerto donde escucha el servidor
PORT=8080

# Entorno de ejecucion: development | production
NODE_ENV=development

# Cadena de conexion a MongoDB
MONGO_URL=

# Clave secreta para firmar los JWT
JWT_SECRET=

# Tiempo de expiracion del token (ej: 1h, 30m, 7d)
JWT_EXPIRES_IN=1h